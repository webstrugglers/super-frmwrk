#ifndef SERVER_HPP
#define SERVER_HPP

class Server {
     private:
     public:
    Server();
    Server(int port);

    // TODO:
    ~Server();

    void start(int port);

     private:
};

#endif  // !SERVER_HPP
